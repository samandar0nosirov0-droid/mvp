{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", "apps/web/.next/**"]
    },
    "dev": {
      "cache": false
    },
    "lint": {},
    "typecheck": {},
    "test": {}
  }
}
